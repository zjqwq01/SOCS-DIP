conditioning:
  method: ps
  params:
    scale: 0.3

data:
  name: ffhq
  root: ./data/samples/

measurement:
  operator:
    name: high_dynamic_range
    # in_shape: !!python/tuple [1, 3, 256, 256]
    scale: 2
  
  noise: 
    name: gaussian
    sigma: 0.05